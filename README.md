# extended-bash-shell
Extension of bash written in C with basic bash commands and additional features such as: Client-server networking, custom process management with signal-driven cleanup, recursive directory traversal with filters, pipe and job support, and modular extensible built-ins implemented with low-level system calls.
